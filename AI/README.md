# [README](README.md)
The **AI** directory exists at the top level of this repository. 

Although it is very tempting to keep placing key infrastructure inside 'hidden' directories (`./claude` and so on), this is actually a huge interoptability nightmare (especially when you want to easily version control things, such as agent markdown files) in a __NON WORKSPACE LOCAL__ type of way.

# See Also
## [Agents/](Agents/README.md)

